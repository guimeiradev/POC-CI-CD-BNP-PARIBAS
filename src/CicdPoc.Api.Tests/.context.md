<context path="src/CicdPoc.Api.Tests" updated="2026-07-01">

xUnit test project for `CicdPoc.Api`, run by the `Test` stage of the root
`Jenkinsfile` (`dotnet test src/CicdPoc.sln`).

## Summary

- CicdPoc.Api.Tests.csproj — `Microsoft.NET.Sdk` test project targeting `net10.0`, non-packable; references `CicdPoc.Api` and depends on `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`, `Microsoft.AspNetCore.Mvc.Testing`, and `coverlet.collector` (for code-coverage collection, relevant to the planned Fase 3 SonarQube coverage gate).
- HealthEndpointTests.cs — `HealthEndpointTests`, an `IClassFixture<WebApplicationFactory<Program>>` integration test that calls `GET /health` and asserts a success status code and that the response body contains `"healthy"`.

## Constraints

- MUST reference `CicdPoc.Api.csproj` as a `ProjectReference` — `WebApplicationFactory<Program>` needs the API's `Program` class at compile time.

</context>
